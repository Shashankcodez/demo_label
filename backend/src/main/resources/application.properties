# ===================================================================
# LabelCheck Backend Configuration (SIH26034)
# ===================================================================

# Application Identity
spring.application.name=labelcheck-backend

# Server Configuration
server.port=8080
server.address=0.0.0.0

# Servlet Multipart Configuration (Max 10MB per label image)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Temporary Image Upload Directory
app.upload.dir=uploads

# Tesseract OCR Configuration (Fallback / Evidence)
app.ocr.enabled=${APP_OCR_ENABLED:true}
app.ocr.tesseract.datapath=${TESSERACT_DATA_PATH:tessdata}
app.ocr.tesseract.language=${TESSERACT_LANGUAGE:eng}

# Vision AI Configuration (Primary Extraction Source — Groq Vision / Qwen 3.6 27B)
app.ai.enabled=${AI_ENABLED:true}
app.ai.provider=${AI_PROVIDER:groq}
app.ai.api-key=${GROQ_API_KEY:${AI_API_KEY:}}
app.ai.base-url=${AI_BASE_URL:https://api.groq.com/openai/v1}
app.ai.model=${AI_MODEL:qwen/qwen3.6-27b}
app.ai.timeout-seconds=${AI_TIMEOUT_SECONDS:30}
app.ai.max-retries=${AI_MAX_RETRIES:1}
app.ai.max-completion-tokens=${AI_MAX_COMPLETION_TOKENS:2500}
app.ai.reasoning-effort=${AI_REASONING_EFFORT:none}
app.ai.temperature=${AI_TEMPERATURE:0.2}

# H2 Database Configuration (File-based local storage surviving restarts)
spring.datasource.url=jdbc:h2:file:./data/labelcheck;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Spring Data JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# H2 Console (Disabled for production/security)
spring.h2.console.enabled=false

# CORS Allowed Origin Patterns (Localhost and Local Area Network for mobile testing)
labelcheck.cors.allowed-origin-patterns=http://localhost:5173,http://127.0.0.1:5173,http://192.168.0.122:5173,http://192.168.*.*:5173,http://10.*.*.*:5173,http://172.16.*.*:5173

# Standard Logging Configuration
logging.level.root=INFO
logging.level.com.labelcheck=INFO
logging.level.org.springframework.web=INFO
